@page
@model oficinaCovid.App.Frontend.Pages.OficinaModel
@{
}
<h3 id="hey">Módulo de oficinas</h3>
<table class="table table-hover">
    <thead>
        <th>Número</th>
        <th>Aforo</th>
        <th>Secretario</th>
    </thead>
    <tbody>
        @foreach (var oficina in Model.oficinas)
        {
            <tr>@oficina.numero</tr>
            <tr>@oficina.aforo</tr>
            <tr>@oficina.secretario</tr>
        }
    </tbody>
</table>
<form action="" method="post" class="col-6 col-md-6 col-sm-6 mt-3">
    <div class="form-group">
        <input asp-for="gobernacion.nombre" id="gobernacion" class="form-control" disabled />
    </div>
    <div class="form-group">
        <label asp-for="oficina.numero">Número de oficina</label>
        <input type="text" asp-for="oficina.numero" class="form-control"/>
    </div>
    <div class="form-group">
        <label asp-for="oficina.aforo">Aforo</label>
        <input type="number" asp-for="oficina.aforo" class="form-control">
    </div>
    <div class="form-group">
        <label asp-for="oficina.secretario">Secretario</label>
        <select asp-for="oficina.secretario" id="secretarios" class="form-control"></select>
    </div>
    <div class="form-group">
        <button class="btn btn-success">Agregar oficina</button>
    </div>
</form>